import React,{useEffect,useState} from 'react';
import {Languages,Moon,Sun} from 'lucide-react';
export function CityClock({language}){const [now,setNow]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(id)},[]);const time=new Intl.DateTimeFormat(language==='en'?'en-US':'es-MX',{timeZone:'America/Mexico_City',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(now);return <span className="city-clock" title="America/Mexico_City">CDMX {time}</span>}
export default function SiteControls({language,setLanguage,theme,setTheme}){return <div className="site-controls"><label><Languages size={16}/><span className="sr-only">Language</span><select value={language} onChange={e=>setLanguage(e.target.value)} aria-label="Site language"><option value="es">Español</option><option value="en">English</option></select></label><button type="button" onClick={()=>setTheme(theme==='dark'?'light':'dark')} aria-label="Cambiar modo de color">{theme==='dark'?<Sun size={17}/>:<Moon size={17}/>}</button></div>}
